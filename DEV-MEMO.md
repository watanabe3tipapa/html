# DEV-MEMO

このサイトの開発メモです。

## 概要

- Pagefind による全文検索 + コンテンツページ（`content/`）の静的サイト
- GitHub Actions でビルド・デプロイ
- ルート `index.html` がランディングページ（検索 + コンテンツ一覧）

## ディレクトリ構成

```
.
├── index.html                     # ランディングページ（検索 + コンテンツ一覧）
├── pagefind.yml                    # Pagefind 設定（glob: **/*.html）
├── content/                        # コンテンツページ（HTML）
└── .github/workflows/
    ├── pagefind.yml                # Pagefind ビルド & Pages デプロイ（唯一のデプロイ元）
    ├── update-readme-html-index.yml
    ├── update-readme-structure.yml
    └── update-content-index.yml   # index.html のコンテンツ一覧を自動生成
```

## デプロイ構成

- デプロイは `.github/workflows/pagefind.yml` に一本化
- ルート全体（index.html / content/ / pagefind/）を Pages アーティファクトとして公開
- `pages.yml` は content/ のみを公開して競合するため削除済み
- `pagefind/` はビルド時に生成されるため `.gitignore` で管理対象外

## コンテンツ追加手順

1. `content/` に HTML ファイルを追加
2. `<title>` タグにページタイトルを設定（一覧表示に使用）
3. コミット & プッシュ
4. GitHub Actions が自動で:
   - index.html のコンテンツ一覧を更新
   - pagefind インデックスを再生成
   - GitHub Pages にデプロイ

## ローカル実行

```bash
# プレビュー用サーバー（Pagefind は HTTP 経由で動作）
python3 -m http.server 8000

# Pagefind インデックスの再生成
pagefind
```

## メモ

- index.html のコンテンツ一覧は `<!-- CONTENT:START -->` / `<!-- CONTENT:END -->` の間を Actions が自動更新する
- README の構造セクションも Actions が自動更新する
- GitHub Pages の URL は subpath 配下（例: `https://<user>.github.io/html/`）
  - pagefind は相対パス（`./pagefind/`）で参照する

## 実装記録

### 2026-08-28: lp/ → content/ 改名・デプロイ一本化・コンテンツ一覧機能

背景にあった問題と対応をまとめる。

#### 実施内容 1: ディレクトリ改名

- `lp/` → `content/` に `git mv` で改名
- 影響範囲を確認のうえ変更:
  - `.github/workflows/pages.yml` が `path: './lp'` を参照 → 後述の通り本ファイルは削除
  - LP ページ内のリンクはすべて外部 URL だったため修正不要
  - README は GitHub Actions が自動再生成するため手動変更不要

#### 実施内容 2: デプロイ競合の解消

GitHub Pages に 2 つのデプロイワークフローが存在し競合していた。

| ワークフロー | アップロード内容 | 状態 |
|-------------|-----------------|------|
| `pages.yml` | `./lp`（content/）のみ | **削除** |
| `pagefind.yml` | ルート全体（index.html / content/ / pagefind/） | 一本化 |

- 削除前は `pages.yml` が実効デプロイとなり、`/pagefind/*` が 404（検索不能）、root index.html も公開されていなかった
- `pagefind.yml` に一本化することで、ランディングページ + コンテンツ + 検索が一体で公開される

#### 実施内容 3: 不要ファイルの削除

- `content/index.html`（ルート index.html の古い複製・検索ページ）を削除
- `content/pagefind.yml`（ルート pagefind.yml（ワークフロー）の複製）を削除

#### 実施内容 4: index.html にコンテンツ一覧を追加

- `<section class="content-section">` を検索ボックス直下に追加
- 一覧は `<!-- CONTENT:START -->` / `<!-- CONTENT:END -->` マーカー間を GitHub Actions が自動更新
- 表示形式: `content/*.html` の `<title>` をリンクテキスト、ファイル名を補助表示するカード型リスト
- スタイルは既存ダークテーマ（`:root` 変数）に合わせ `.content-list` を追加

生成される HTML 例:

```html
<section class="content-section">
  <h2>Contents</h2>
  <!-- CONTENT:START -->
  <!-- This section is auto-generated by GitHub Actions. Do not edit manually. -->
  <ul class="content-list">
    <li><a href="content/architecture.html"><span class="c-title">Pagefind Setup Architecture</span><span class="c-file">architecture.html</span></a></li>
    ...
  </ul>
  <!-- CONTENT:END -->
</section>
```

#### 実施内容 5: 自動更新ワークフローの新設

- `.github/workflows/update-content-index.yml` を新規作成
- 仕組み（README 自動更新と同じパターン）:
  1. `content/*.html` をソート収集
  2. 各ファイルの `<title>` を正規表現で抽出
  3. マーカー間に一覧の HTML を注入して index.html に書き戻し
  4. `[skip ci]` 付きコミット & プッシュ
- 一覧が空の場合は `content-empty` メッセージを表示

#### 実施内容 6: Pagefind インデックスの再構築

- ローカルで `rm -rf pagefind && pagefind` を実行
- 検索結果 URL が `content/...` を指すよう再インデックス（24 ページ / 4,811 語）

#### 実施内容 7: index.html を Neo-Brutalism スタイルにリデザイン

- 参考サイト: https://release-tracker.watanabe3ti.workers.dev
- これまでのダークテーマ（`#0b0f19` 背景）を廃止し、Neo-Brutalism に刷新

デザイントークン:

| 要素 | 値 |
|------|-----|
| 背景 | `#f5f0e8`（クリーム） |
| 文字/枠線 | `#111`（ブラック） |
| アクセント | `#ffe66d`（イエロー） |
| カード面 | `#fff` |
| 枠線 | `4px solid #111` |
| シャドウ | `6〜10px 適用0 #111` のハードシャドウ |

構成:

- `.header`: イエロー背景 + 黒枠 + ハードシャドウ（ブランドタグ `▼ Pagefind`）
- `.search-panel`: 白パネル + 黒枠 + シャドウ、Pagefind UI を内包
- `.content-section`: マーカー生成の一覧をカード型リンク（黒枠 + シャドウ + ホバーで浮き上がり）にスタイル
- `.footer`: 控えめなセンターテキスト

Pagefind の Default UI は CSS 変数と `!important` オーバーライドで同テーマに統一（角丸なし・黒枠・イエローボタン）。

#### 今後の URL 構造（デプロイ後）

```
/https://<user>.github.io/html/
├── /                        # root index.html（検索 + コンテンツ一覧）
├── /content/xxx.html        # 各コンテンツページ
└── /pagefind/               # 検索インデックス
```